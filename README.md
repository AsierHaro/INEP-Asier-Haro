Per configurar la base de dades utilitzem un arxiu de configuració extern. Cada linia de l'arxiu dona les dades per cada un dels ambits necessaris.

Aclaracio! El programa principal es troba a l'arxiu INEP!.cpp
